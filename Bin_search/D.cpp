#include <iostream>

int main() {
    int N;
    std::cin >> N;
    int a[N];
    for (int i = 0; i < N; ++i) std::cin >> a[i];
    int K;
    std::cin >> K;
    for (int i = 0; i < K; ++i) {
        int L, R;
        std::cin >> L >> R;
        
    }
}